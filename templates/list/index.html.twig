<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>
			{% block title %}Swaply
			{% endblock %}
		</title>
		<link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
		<link rel="stylesheet" href="./assets/styles/app.css">
		<script src="https://cdn.tailwindcss.com"></script>

		{% block javascripts %}
			{% block importmap %}
				{{ importmap('app') }}
			{% endblock %}
		{% endblock %}
	</head>

	{% block body %}
		{% include 'base.html.twig' %}
		<h1 class="text-center text-3xl font-bold mb-6 text-white">Listes</h1>
		<ul class="bg-zinc-900 rounded-3xl w-96 h-2/3 mt-10 mb-10 justify-center mx-auto p-10">
			{% for list in lists %}
				<li>
					<img src="{{ asset('assets/uploads/covers/' ~ list.cover) }}" alt="Cover" class="w-80 rounded-2xl">
					<p class="text-center text-xl font-bold text-white mt-3 mb-3">{{ list.title }}</p>
					<p class="text-center italic text-zinc-400 mb-3">{{ list.date|date('d/m/Y - H:i') }}</p>
					<p class="mb-3">{{ list.description }}</p>
					<div class="flex justify-center space-x-2 h-10">
						<a type="button" href="{{ list.website }}" class="text-white bg-zinc-800 hover:bg-zinc-700 font-medium rounded-2xl text-sm p-5 border-zinc-700 flex items-center justify-center">Accéder</a>
						<a class="flex bg-indigo-700 hover:bg-indigo-800 rounded-2xl text-white font-bold p-5 items-center justify-center">{{ list.likes }} 💜</a>
					</div>
				</li>
			{% else %}
				<p>Aucune liste trouvé ! :'(</p>
			{% endfor %}
		</ul>
	{% endblock %}
</html>
